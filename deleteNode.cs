public class Solution {
    public TreeNode DeleteNode(TreeNode root, int key) {
        if(root == null) return root;
        List<TreeNode> stack = new List<TreeNode>();
        stack.Add(root);
        TreeNode curr;
        while(stack.Count > 0){
            curr = stack[stack.Length];

        }

    }
}
